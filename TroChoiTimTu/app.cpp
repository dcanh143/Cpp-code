#include "iostream"
using namespace std;

int main() {
    int n, m;
    cin >> m >> n;
    char a[m][n];
    for(int i = 0; i < m; ++i) {
        for(int j = 0; j < n; ++j) {
            cin >> a[i][j];
        }
    }
    
}